Scripts, that help getting network statistics during TPC-DS tests

Included:
	ethtool statistic
	nstat statistic
	/sys/class/net/.. statistic
	atop output

Usage:

# cd /root
# git clone https://github.com/johnli139/network2
# cd network2
change host names in file "hosts"
change IF variable in files "begin.sh" and "end.sh"
set up passwordless root access via ssh to needed hosts
# ./run.sh
